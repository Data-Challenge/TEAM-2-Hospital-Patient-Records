# TEAM-2-Hospital-Patient-Records

### Installation
Clone or Download ZIP this project to your local machine.

Open terminal, go to directory you want for this project
(If you downloaded as ZIP file, extract it).

Run `pip install -r requirement.txt` on terminal to download all dependencies that requires to run this project (You can use virtual environment to avoid conflict between global and local packages)

That's it for installation!
